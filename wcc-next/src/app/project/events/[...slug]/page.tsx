import { Fragment } from "react";
import { NextPage } from "next";

interface EventItemFilterProps {}

const EventItemFilter: NextPage<EventItemFilterProps> = (props) => {
  return (
    <Fragment>
      <h1>filter</h1>
    </Fragment>
  );
};
export default EventItemFilter;
